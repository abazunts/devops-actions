name: File Change Based Workflow in Monorepo

on:
  push:
    branches:
      - develop
    paths:
      - 'service1/**'
      - 'service2/**'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Build service1
        if: contains(github.event.head_commit.modified, 'service1')
        run: |
          echo "Building service1..."
          cd service1
          # Add build commands for service1 here

      - name: Build service2
        if: contains(github.event.head_commit.modified, 'service2')
        run: |
          echo "Building service2..."
          cd service2
          # Add build commands for service2 here

  deploy:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Deploy service1
        if: contains(github.event.head_commit.modified, 'service1/')
        run: |
          echo "Deploying service1..."
          cd service1
          # Add deployment commands for service1 here

      - name: Deploy service2
        if: contains(github.event.head_commit.modified, 'service2/')
        run: |
          echo "Deploying service2..."
          cd service2
          # Add deployment commands for service2 here
